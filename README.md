# FightAnalyzer

---

# 📚 **Методические рекомендации по тактико-техническому анализу единоборств с помощью FightAnalyzer**  
*(Автоматизированный анализ видео на основе компьютерного зрения и машинного обучения)*  
**Автор:** Ермаков А.В., 2025 г.  
**Версия:** 1.0  
**Целевая аудитория:** тренеры, спортсмены, судьи, исследователи

---

## 1. Введение

В современных единоборствах (фехтование, каратэ, бокс, кикбоксинг, ММА, тхэквондо и др.) победа  зависит  от **тактической осведомлённости**, **умения прогнозировать действия соперника**, **контроля дистанции и баланса**.

Традиционный подход — визуальный анализ тренером — субъективен и ограничен вниманием, усталостью, опытом. FightAnalyzer решает эту проблему:

✅ **Объективные метрики** — без человеческой погрешности  
✅ **Автоматизация** — отчёт за 2–3 минуты после загрузки видео  
✅ **Глубокая аналитика** — не только «кто быстрее», а «кто эффективнее»  
✅ **Низкий порог входа** — не требуется видеокамера с датчиками, только обычное видео

---

## 2. Как работает система

### Этапы анализа:

1. **Детекция спортсменов**  
   → Используется YOLOv8n — нейросеть для обнаружения людей в реальном времени.  
   → Фильтрация: только «человекообразные» объекты с соотношением сторон 0.2–1.0 и высотой >15% кадра.

2. **Трекинг и калибровка**  
   → После выбора спортсменов система фиксирует их начальные позиции.  
   → Калибровка масштаба:  
     \[
     \text{scale\_px\_per\_m} = \frac{\text{высота бокса (px)}}{\text{рост спортсмена (м)} \times 0.8}
     \]  
     *(0.8 — приблизительная доля роста, приходящаяся на туловище)*

3. **Расчёт метрик**  
   → Все координаты переводятся из пикселей в метры.  
   → Скорость и ускорение вычисляются через численное дифференцирование (`np.gradient`).  
   → Для устранения шума — сглаживание по 5 кадрам.  
   

4. **Формирование отчёта**  
   → CSV — все метрики по кадрам  
   → PDF — интерпретация + графики + таблицы  
   → Видео — аннотация с дистанцией, скоростью, ускорением, шириной стойки

---

## 3. Расшифровка метрик в CSV-файле (`analysis.csv`)

| Метрика | Единица | Как рассчитана | Что означает |
|--------|---------|----------------|--------------|
| `frame` | — | номер кадра | Порядковый номер кадра |
| `time_sec` | с | `frame / fps` | Абсолютное время |
| `dist_lr_m` | м | `√[(xₗ−xᵣ)² + (yₗ−yᵣ)²] / scale_px_per_m` | Дистанция между центрами туловищ |
| `v_left_mps`, `v_right_mps` | м/с | `∥Δr/Δt∥ / 100` | Скорость спортсмена (сглаженная) |
| `a_left_mps2`, `a_right_mps2` | м/с² | `∥d²r/dt²∥ / 10000` | Ускорение (сглаженное) |
| `x_left`, `y_left` | пиксели | центр бокса YOLO | Координаты левого спортсмена |
| `zone_left`, `zone_right` | — | 3×3 сетка по `(x, y)` | Зона ринга (top-left, center и т.д.) |
| `stance_width_left_cm` | см | `(x₂−x₁) / scale_px_per_m × 100` | Ширина стойки (≈ расстояние между ногами) |
| `approach` | 0/1 | `1`, если дистанция уменьшилась | Сближение в данный кадр |
| `acceleration_profile` | 0/1/2 | `2` — `|a| > 1.5`, `1` — `0.8 < |a| ≤ 1.5` | 2 = удар рукой, 1 = удар ногой, 0 = движение/пауза |
| `move_toward_left_m` | м | проекция смещения на линию к сопернику | **>0 — атака**, **<0 — уход** |

> 🔹 **Важно**:  
> - `move_toward_left_m` и `move_toward_right_m` — **независимы**.  
> - Если левый сделал шаг вперёд на 0.15 м, а правый стоял — `move_toward_left_m = +0.15`, `move_toward_right_m = 0`.  
> - Если оба сошлись — оба значения >0.

---

## 4. Как читать PDF-отчёт (`report.pdf`)

### 1. Описательная статистика  
**Цель**: оценка общей активности.  
**Пример из отчёта**:

|  | dist_lr_m | v_left_mps | v_right_mps | a_left_mps2 | a_right_mps2 |
|---|---|---|---|---|---|
| **mean** | 2.27 | 0.29 | 0.28 | 0.01 | 0.01 |
| **max** | 2.46 | 3.38 | 2.40 | **0.64** | 0.30 |

→ Левый развивает **большую пиковую скорость и ускорение** — агрессивный стиль.

---

### 2. Время в зонах и атаки  
**Цель**: тактическое расположение.  
**Пример**:

| Зона | Время (с) | Атаки (левый) | Атаки (правый) |
|------|-----------|----------------|----------------|
| mid-left | 14.87 | 1 | 0 |
| mid-right | 0 | 0 | 6 |

→ Правый атакует **только из правой половины пространства** — предсказуемая тактика.  
→ Левый почти всё время в mid-left — пассивная позиция или удержание наиболее благоприятной позиции.

---

### 3. Совпадение ускорения и направления  
**Цель**: определение стиля ведения боя.  
**Формулы**:
- **Атака** = `move > 0` и `|a| ≥ 0.8·aₘₐₓ`  
- **Контратака** = `move < 0` и `|a| ≥ 0.8·aₘₐₓ`  
- **Оборона** = `move < 0` и `|a| < 0.8·aₘₐₓ`

**Пример**:

| Спортсмен | Атака | Контратака | Оборона |
|-----------|-------|-------------|----------|
| Левый | 100.0% | 0.0% | 44.2% |
| Правый | 33.3% | 66.7% | 42.2% |

→ Левый **только атакует**, но почти не уходит — высокий риск.  
→ Правый — **контратакующий стиль**: отступает → ускоряется → атакует.

---

### 4. Плотность боя (атак/мин)  
**Цель**: выявление «горячих» периодов.

| Период | Левый | Правый | Суммарно |
|--------|-------|--------|----------|
| 1/3 | 0.0 | 17.8 | 17.8 |
| 2/3 | 8.9 | 17.8 | **26.7** |
| 3/3 | 0.0 | 17.8 | 17.8 |

→ Пик активности — **во 2-й трети** (возможно, после отдыха в 1-й трети).  
→ Левый атакует **только во 2-й трети** — (возможно, таков тактический план на бой или склонность спортсмена к определённым темпо-ритмовым структурам).

---

### 5. Ширина стойки при атаке  
**Цель**: оценка баланса и устойчивости.

| Спортсмен | min | mean | max | mode |
|-----------|-----|------|-----|------|
| Левый | 124.2 | 124.2 | 124.2 | 124.2 |
| Правый | 102.6 | 112.2 | 131.2 | 102.6 |

→ У левого — **фиксированная ширина стойки** (124.2 см) во всех атаках → стабильная техника, но возможно, ограничения в тактической подготовленности.  
→ У правого — динамика от 102.6 до 131.2 см → адаптация под действие соперника или нестабильность техники.

---

### 6. Манера ведения боя по третям  
**Цель**: динамика тактики.

| Треть | Атакующая | Контратакующая | Оборонительная |
|------|-----------|------------------|-----------------|
| 1/3 | 0% | 0% | 100% |
| 2/3 | 1% | 0% | 99% |
| 3/3 | 0% | 0% | 100% |

→ Левый **постоянно в обороне**, даже в активной трети — возможно, тактическая пассивность.

---

### 7. Текстовое описание (автоматически сгенерировано)

> **Анализ боя**:  
> • Средняя скорость: левый — 0.29 м/с, правый — 0.28 м/с.  
> • Макс. ускорение: левый — **0.64 м/с²**, правый — 0.30 м/с².  
> • Тактическое поведение:  
> — Левый сделал **оборонительных** движений на **0.07 м**.  
> — Правый сделал **оборонительных** движений на **0.09 м**.  
> • Минимальная дистанция: **2.05 м** — вероятно, моменты атак.  
> • Максимальная дистанция: **2.46 м** — вероятно, уходы или паузы, безопасная дистанция.

**Интерпретация**:  
- Несмотря на высокое пиковое ускорение, левый почти не атакует.  
- Правый контролирует дистанцию — держит соперника на расстоянии 2.05–2.46 м.  
- Оба спортсмена — в оборонительной манере, но правый использует контратаки (66.7%).

---

## 5. Практические рекомендации для тренеров

### 🔹 Диагностика стиля
| Стиль | Признаки |
|-------|----------|
| **Атакующий** | `move > 0` в 70%+ атак, высокая плотность, `stance_width` стабильна |
| **Контратакующий** | `move < 0` → `a > 0.8·aₘₐₓ`, низкая плотность, но высокая эффективность |
| **Оборонительный** | `move < 0` и `a < 0.5·aₘₐₓ`, ширина стойки растёт со временем |

### 🔹 Оценка усталости
- **Деградация скорости** во 2/3 → 3/3: `v_mean` падает на >30%  
- **Рост ширины стойки**: `Δstance > +0.1 см/с` — признак снижения устойчивости  
- **Снижение плотности атак** — даже при сохранении `move_toward`

### 🔹 Подготовка к сопернику
1. Получите `report.pdf` соперника.  
2. Найдите его **доминирующую зону атак** (табл. 2).  
3. Отработайте **закрытие этой зоны** + **провокацию в слабые зоны**.  
4. Если он контратакующий (табл. 3) — используйте **фейки без ускорения**.

---

## 6. Ограничения и рекомендации

| Проблема | Решение |
|---------|---------|
| Калибровка по росту → ±10% погрешности | Используйте **разметку площадки** (если видна) для точной калибровки (`1 м = X px`) |
| Перекрытия спортсменов → сбой трекинга | Размечайте таких кадров вручную или исключайте из анализа |
| Не учитывается направление атаки (в голову/туловище) | FightAnalyzer фокусируется на **тактике перемещения**, а не технических особенностях |
| Не заменяет эксперта | Используйте отчёт как **основу для диалога**, а не как окончательный вердикт |

> ✅ **Лучшая практика**:  
> — Анализируйте **не один бой**, а серию (3–5) — так выявите устойчивые паттерны.  
> — Сравнивайте «до/после» тренировочного цикла.  
> — Всегда смотрите **видео + отчёт вместе**.

---

## 7. Заключение

**FightAnalyzer — это не «робот-тренер», а цифровой ассистент**, который:

- ✅ Показывает **что происходит** (метрики),  
- ✅ Подсказывает **где искать проблему** (таблицы),  
- ✅ Даёт основания для **обсуждения тактики** (термины: «контратака», «плотность», «ширина стойки»).

Чем раньше вы начнёте использовать объективные данные — тем раньше спортсмен начнёт побеждать 

